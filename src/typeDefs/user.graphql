type Query {
  user: User! @requiresAuth
  users: [User!]! @requiresAuth
}

type User {
  id: Int!
  nickname: String!
  sentFriendRequests: [User!]!
  receivedFriendRequests: [User!]! 
}

type Mutation {
  sendFriendRequest(friendId: Int!): User! @requiresAuth
}