type Query {
  user: User! @requiresAuth
  users: [User!]! @requiresAuth
}

type User {
  id: Int!
  nickname: String!
  sentFRequests: [User!]!
  receivedFRequests: [User!]!
  receivedFReqFromMe: Boolean!
  # isFriendsWithMe: Boolean!
}

type Mutation {
  sendFRequest(friendId: Int!): User! @requiresAuth
}